{
  "$schema": "./providers.schema.json",
  "version": 1,
  "description": "Open discovery registry of Fiber LSP providers. A client fetches this, then calls each provider's /lsp/v1/info to compare live quotes. Add your provider via PR.",
  "providers": [
    {
      "name": "RouteKit Reference LSP (testnet)",
      "base_url": "http://127.0.0.1:8080",
      "chain": "testnet",
      "operator": "fiber-lsp-kit",
      "note": "Local reference server bundled with this repo. Points at your own FNN node."
    }
  ]
}
