{
  "name": "fiber-lsp-kit",
  "version": "0.0.0",
  "private": true,
  "description": "Liquidity-service (LSP) protocol, reference server, and client SDK for Fiber Network \u2014 buy inbound liquidity, per asset.",
  "license": "MIT",
  "engines": {
    "node": ">=20.6"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/protocol packages/fiber packages/auth packages/registry packages/lsp-server packages/client packages/prover-linked",
    "demo:lsp": "npm run build && node scripts/demo/servers/lsp.mjs",
    "demo:merchant": "npm run build && node scripts/demo/servers/merchant.mjs",
    "demo:customer": "npm run build && node scripts/demo/servers/customer.mjs",
    "demo:invoice": "node scripts/demo/actions/request-invoice.mjs",
    "demo:pay": "node scripts/demo/actions/pay.mjs",
    "demo:direct-invoice": "node scripts/demo/actions/direct-invoice.mjs",
    "demo:direct-pay": "node scripts/demo/actions/direct-pay.mjs",
    "demo:rent": "node scripts/demo/actions/stream-rent.mjs",
    "demo:e2e": "npm run build && node scripts/demo/e2e.mjs",
    "typecheck": "tsc -b packages/protocol packages/fiber packages/auth packages/registry packages/lsp-server packages/client packages/prover-linked",
    "test": "npm run build && node --import tsx --test packages/*/test/*.test.ts",
    "test:live": "npm run build && node --import tsx --test packages/*/test/integration/*.test.ts",
    "server": "npm run build && npm -w @fiberlsp/server run start",
    "release": "node scripts/release/build-artifacts.mjs",
    "prover:build": "cargo build --release --manifest-path tools/linkage-prover/Cargo.toml"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "circomlib": "^2.0.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "keywords": [
    "fiber",
    "ckb",
    "nervos",
    "lsp",
    "liquidity",
    "payment-channels",
    "lightning"
  ]
}
