services:
  - type: web
    name: fiber-lsp-kit-demo
    runtime: node
    buildCommand: npm ci && npm run build && npm run demo:hosted:artifacts
    startCommand: npm run demo:hosted
    healthCheckPath: /health
    autoDeployTrigger: commit
